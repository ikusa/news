# Migration `20190725012707-make-author-required`

This migration has been generated by Joshua at 7/25/2019, 1:27:07 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Article"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"title" TEXT NOT NULL DEFAULT '' ,"content" TEXT NOT NULL DEFAULT '' ,"author" TEXT NOT NULL  REFERENCES User(id),PRIMARY KEY ("id"));

INSERT INTO "new_Article" ("id","createdAt","updatedAt","title","content","author") SELECT "id","createdAt","updatedAt","title","content","author" from "Article"

DROP TABLE "lift"."Article";

ALTER TABLE "lift"."new_Article" RENAME TO "Article";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190724221654-init..20190725012707-make-author-required
--- datamodel.dml
+++ datamodel.dml
@@ -6,8 +6,12 @@
 generator photon {
   provider = "photonjs"
 }
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
 model User {
   id       String    @default(cuid()) @id @unique
   email    String    @unique
   name     String?
@@ -19,6 +23,6 @@
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   title     String
   content   String
-  author    User?
+  author    User
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190725012707-make-author-required)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190725012707-make-author-required'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
