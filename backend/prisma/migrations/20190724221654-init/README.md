# Migration `20190724221654-init`

This migration has been generated by Joshua at 7/24/2019, 10:16:54 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "dev"."Post";

CREATE TABLE "dev"."Article"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"title" TEXT NOT NULL DEFAULT '' ,"content" TEXT NOT NULL DEFAULT '' ,"author" TEXT   REFERENCES User(id),PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "dev"."Article.id._UNIQUE" ON "Article"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190624162221-init..20190724221654-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,27 +1,24 @@
 datasource db {
   provider = "sqlite"
-  url      = "file:dev.db"
-  default  = true
+  url      = "file:./dev.db"
 }
 generator photon {
   provider = "photonjs"
-  output   = "../node_modules/@generated/photon"
 }
 model User {
-  id    String  @default(cuid()) @id @unique
-  email String  @unique
-  name  String?
-  posts Post[]
+  id       String    @default(cuid()) @id @unique
+  email    String    @unique
+  name     String?
+  articles Article[]
 }
-model Post {
+model Article {
   id        String   @default(cuid()) @id @unique
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
-  published Boolean
   title     String
-  content   String?
+  content   String
   author    User?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190724221654-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190724221654-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
